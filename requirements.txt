# AiCoach Project Requirements
# Install with:  pip install -r requirements.txt

# Garmin Connect download + SQLite storage (pulls in SQLAlchemy, garth,
# fitfile, tcxfile, idbutils, tqdm, python-dateutil, etc. as dependencies).
garmindb==3.6.7

# Direct parsing of FIT files for training analysis (used by the coach scripts).
fitparse>=1.2.0

# Secure credential storage (Garmin password kept in the OS credential manager).
keyring>=25.0.0
